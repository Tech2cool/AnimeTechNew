import { StyleSheet, Text, View } from 'react-native'
import React, { useEffect } from 'react'
import { TouchableOpacity } from 'react-native-gesture-handler'

const Pagination = () => {
    // const {pages, setPages, fetchAnime, isSearch=false} = params
    // const {currentPage, totalPages, pagesArray} = pages

    // const setPagesArr= (currentPage,totalPages)=>{
    //     let arr=[];
    //     for(i= parseInt(currentPage); i<=parseInt(totalPages); i++){
    //         arr.push(i)
    //     }
    //     // console.log(arr)
    //     return arr
    // }
    // useEffect(()=>{
    //     const arr = setPagesArr(currentPage,totalPages)
    //     setPages(prev=>({
    //         ...prev,
    //         pagesArray:arr,
    //     }))
    // },[])
  return (
    <View style={{marginVertical:150}}>
        <Text>ssfs</Text>
    </View>
  )
}

export default Pagination

const styles = StyleSheet.create({})